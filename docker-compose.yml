version: '3'

services:
  database-s: # Service
    container_name: database-c # Container
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: music
      MYSQL_PASSWORD: root