import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
  backItemText: {
    fontSize: "1.1rem",
    fontWeight: "500"
  },
  backItem: {
    display: "inline-flex",
    alignItems: "center"
  }
}));

export default useStyles;
